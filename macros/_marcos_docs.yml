macros:
  - name: cent_to_dollar
    description: A macro that converts cents to dollars and rounds to the input decimal places, or 2 if none is provided
    arguments:
      - name: column_name
        type: string
        description: The name of the column you want to convert
      - name: decimal
        type: integer
        description: number of decimal places, defaults to 2
        